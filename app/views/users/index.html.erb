<div class="content">
	<div class="mid">
		<div class="logo_container">
			<%= link_to image_tag("/logo.png", :alt => "tracklink logo", :id => "logo"), root_path %>
		</div>
		<div class="navigation_container">
			<% if logged_in? %>
				<%= link_to "Index", root_path, :class => "nav" %>
				<%= link_to "Users", users_path, :class => "nav" %>
				<%= link_to "History", history_path, :class => "nav" %>
				<%= link_to "Settings", current_user, :class => "nav"  %>
				<%= link_to "Logout", logout_path, method: "delete", :class => "nav"  %>
			<% else %>
				<%= link_to "Login", login_path, :class => "nav"  %>
				<%= link_to "Signup", signup_path, :class => "nav" %>
			<% end %>
		</div>
	</div>
	<div class="mid-wide">
		<div id="search_box_route">
			<table id="user_table">
				<tr>
					<td id="dummy_search_field">
						Enter User
					</td>
					<td id="dummy_search_button">
						Search
					</td>
				</tr>
			</table>
		</div>
		<div id="user_scroll">
			<table id="user_table">  
				<% @users.each do |user| %>
					<tr>
						<td><%= user.email %></td>
						<td><%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
						<td><%= user.admin? ? "Admin" : "User" %></td>
						<td><%= button_to "Promote to Admin",:controller => "users", :action => "promote", :id => user.id %></td>
						<td><%= button_to "Demote to User",:controller => "users", :action => "demote", :id => user.id %></td>
					</tr>
				<% end %>
			</table>
		</div>
	</div>
</div>